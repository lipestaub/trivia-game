version: '3'

services:
  web:
    image: php:apache
   # volumes:
   #  - ./Classes:/var/www/html
    ports:
      - "8080:80"
    depends_on:
      - db

  db:
    image: postgres:latest
    ports:
      - "5432:5432"
        
    volumes:
      - ./PgData:/var/lib/postgresql/data
